# Homepage Config Directory

This directory stores dynamic configurations for Homepage (`settings.yaml`, `services.yaml`, `bookmarks.yaml`, `widgets.yaml`).

These configuration files should be edited to customize your dashboard services, bookmarks, widgets, and general layout.
