# Portainer Data Directory

This directory stores Portainer's database and persistent application data.

> [!IMPORTANT]
> The database and application state files are ignored by Git for security and performance reasons. Only this `README.md` file is tracked to preserve the directory structure.
