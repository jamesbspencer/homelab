# Traefik Certificates Directory

This directory stores Traefik ACME (Let's Encrypt) certificates (e.g., `acme.json`).

> [!IMPORTANT]
> The generated certificates and private keys are ignored by Git for security reasons. Only this `README.md` file is tracked to preserve the directory structure.
